# 契约够用就好 — 面向 Agent 的 DSL 设计手记

## 起源

这不是一篇学术论文，也不是团队设计文档。

它来自一个人跟一个 AI 反复对话、反复卡住、反复顿悟的过程。没有 deadline，没有分工，没有「得看起来像回事」的压力。唯一的标准是：**明天我用的时候它好不好使**。

---

## 问题

人类和 Agent 之间需要沟通代码意图。现有方案都不太对：

| 方案 | 问题 |
|---|---|
| **自然语言** | 人类最自然，但 Agent 容易歧义。「把这个处理一下」——处理成什么？Agent 猜十次可能对五次。 |
| **完整代码** | Agent 最精确，但人类写起来太累。为了描述一个简单意图，要写完整的类型签名 + 函数体。 |
| **JSON Schema / Protobuf** | 精确，但全是样板。而且只描述了数据形状，没描述**变换关系**和**语义角色**。 |
| **Function Calling 的 strict mode** | 强制结构化，但人类在对话里填 JSON 简直是灾难。 |

核心矛盾是：**人类需要轻量，Agent 需要精确**。

一开始我的直觉是「那就设计一个精确的小语言吧」——结果是 DSL 越来越严谨，Agent 倒是理解了，我自己先写不动了。

然后又往反方向试——全用自然语言。Agent 确实懂了，但每次都在一些边缘细节上猜错，比如「输入是一张图片还是一批图片？这个参数是配置项还是数据项？」

反复卡在这两个极端之间。

---

## 顿悟

试了很多次之后，渐渐意识到一个之前一直忽略的事实：

**人类描述一个任务时，真正需要「精确」的东西其实很少。**

大部分时候，一句话就够了：

> 「读这个文件，如果太大就截断，返回摘要。」

Agent 完全能理解这句话。我不需要用类型系统去约束「文件路径必须是字符串」、「返回摘要必须是 Markdown」。

但有一些东西如果不精确说清楚，Agent 一定会搞错：

- 这个参数是「输入数据」还是「配置选项」？
- 这里的返回值是「要么成功要么失败」，还是「成功和失败一起返回」？
- 这个 `"fast"` 是一个模式选择，还是一个日志级别？

也就是说，**自然语言擅长表达「怎么做」，但不擅长表达「什么是什么」**。

反过来，类型系统刚好擅长表达「什么是什么」——角色、形状、可选分支——但用它来描述「怎么做」会非常笨拙。

那为什么不把两者拼起来？

---

## 设计：双层的意图契约

```
Pipeline := ($res Path <- $input Path <- $intent Str) <-
{
  读取路径下的文件
  如果超过 10MB 就先截断
  返回 Markdown 报告
}
```

两层：

**上层（类型层）** — 用三个运算符描述数据的关系和角色。这部分必须是精确的、可解析的，因为人和 Agent 必须共享同一个「什么是什么」的理解。

**下层（块层）** — 用自然语言描述怎么做。这部分不需要被解析，因为它是写给 Agent 读的指令。

两个层合在一起，才是一个完整的意图契约。

---

## 三个运算符

实践中发现，描述数据关系只需要三个运算符，刚好对应类型论里的三种基本结构：

### `A | B` — 选择（余积）

> 「结果要么是 `A` 要么是 `B`。」

对应人类思维里的「或者」、「可能」、「分支」。

```
$ok T | $err E       → 要么成功返回 T，要么报错 E
A | B | never        → A 或 B，不可能有其他情况（never 是零元）
A | B | ...          → A 或 B，但也可能还有别的（... 是开放契约）
```

### `A * B` — 组合（积）

> 「`A` 和 `B` 在一起。」

对应「同时有」、「配置 + 数据」、「参数包」。

```
$cfg.mode "fast" * In * 0.99
→ 配置模式是 fast，加上输入 In，再加上一个置信度阈值 0.99

A * void             → 就是 A（void 是单位元）
(A, B, C)            → 语法糖，展开成 A * (B * C)
```

### `B <- A` — 变换（指数）

> 「把 `A` 变成 `B`。」

对应「映射」、「流水线」、「函数」。

```
Out <- In                       → 从 In 到 Out 的变换
C <- B <- A                     → 柯里化：A → (B → C)
($output Out <- $input In)      → 带角色标注的变换
```

这三个运算符的好处是：**任何人凭直觉就能猜对意思，但在类型论里又有精确的数学含义**。这使得 DSL 既容易被人类接受，又不会被 Agent 误解。

---

## 语义标签系统 — `$tag`

这是整个设计里最关键的部分。它不是来自类型理论，而是来自一个非常实际的痛点：

**当你给 Agent 传两个字符串参数时，它经常搞混哪个是哪个。**

```
# 人的意图：
「用 fast 模式处理图片，输出分割掩码」

# 没有标签，Agent 看到的是：
Out <- "fast" * Image
# "fast" 是模式还是日志级别？Out 是输出路径还是数据？
```

加上标签之后：

```
$res Mask <- $cfg.mode "fast" * $input Image
```

`$cfg.mode` 告诉 Agent：「这个 `"fast"` 是配置模式。」
`$input` 告诉 Agent：「这个是输入数据。」
`$res` 告诉 Agent：「这个是结果。」

标签本质上是在类型系统里嵌入了一个**轻量级的命名空间**。它不需要类型系统去证明合法性，只需要让人类和 Agent 对同一个值有相同的语义理解。

`$a.b.c` 还支持点号嵌套路径，用来表达结构化的语义角色：

```
$meta.id.hash Str    → "这是一个元数据 id 的 hash 字段"
$res.val Out         → "结果的 val 字段是 Out 类型的"
$res.err never       → "结果的 err 分支不可能出现"
```

---

## `{...}` — 不是逃生舱，是核心

我最初也以为 `{...}` 是一个「万不得已才用的 escape hatch」。后来发现不对——**自然语言描述怎么做，才是大多数情况下的主要表达方式**。

类型层只做一件事：**把关键的数据角色和关系说精确**。剩下的，「怎么做」，全部交给自然语言。

```
# 错误的用法 — 类型层拼命描述怎么做
Pipeline := ($read Path * $filter Bool * $summarize Bool * $format Str)

# 正确的用法 — 类型层只说角色，让自然语言说怎么做
Pipeline := ($input Path)
{
  读取这个文件
  如果太大（超过 10MB）就先截取开头和结尾
  然后用 Markdown 格式输出摘要
}
```

这其实是在承认一个事实：**对于「怎么做」这件事，自然语言的表达能力远超任何 DSL**。与其强行把怎么做塞进类型系统，不如把类型系统缩小到只做它擅长的事。

---

## 开放的契约

`...` 表达「这里也可以有别的」：

```
HLSegment[T] := $data T * $next ...
```

这告诉 Agent：「这是一个链式结构，每个节点有数据和一个 next，但 next 未必是同一个类型——你可以继续扩展。」

这种开放性是刻意设计的。不是所有东西在设计之初都能列全，`...` 让契约可以诚实地说「我知道这里不完整」。

同样，`()` 作为 `void` 的语法别名、`void` 和 `never` 作为单位元和零元，这些设计保证了代数恒等式成立：

```
A | never == A      （never 是 sum 的零元）
A * void == A       （void 是 product 的单位元）
```

**但是这些严谨性不是为了理论正确，而是为了让 Agent 不需要记住「特殊情况」**。Agent 读到 `A | never` 应该直接等价于 `A`，不需要绕一个弯。

---

## 设计原则总结

### 1. 契约够用就好

不要试图精确描述所有东西。只需要把「不精确就会搞错」的那些点精确化——主要是数据的**角色**和**关系**。剩下的交给自然语言。

### 2. 运算符要少，语义要直觉

三个运算符 `|`, `*`, `<-`。任何写过代码的人都能凭直觉猜对。不需要学。

### 3. 语义标签是最小粒度的命名

`$tag` 不改变类型，只附加语义角色。人和 Agent 用它来对齐「这个东西是干什么的」。

### 4. 自然语言是主要的实现描述层

`{...}` 不是逃生舱。它是写给 Agent 看的任务说明。大多数「怎么做」用自然语言写比任何 DSL 都清楚。

### 5. 开放优于完备

`...` 允许契约不完整。`()` 和 `void` 互换消除了特例。设计应该能容纳尚未列全的东西。

---

## 实际使用感受

用了一段时间之后，一个有意思的现象是：**类型层越来越短，`{...}` 块越来越自然**。

一开始容易过度设计类型层——恨不得把所有约束都放到类型里。后来发现不需要：

```
# 过度设计
Pipeline := ($input Path * $cfg.chunk_size Int * $cfg.overlap Int * $cfg.lang Str * $output Format)

# 刚好够用
Pipeline := ($input Path)
{
  读取文件，按段落切分（每段不要太大）
  相邻段落之间保留少量重叠
  输出 Markdown 格式
}
```

Agent 从第一个版本得到的是一堆字段名和类型，它不一定知道这些字段实际怎么用。从第二个版本得到的是**一段人能写、Agent 能读的自然语言指令**——反而执行得更准确。

---

## 最后

这不是一篇论文。不会有 formalism、不会有一堆定理、不会有 related work 章节。

这只是一个人在实际使用中反复碰壁之后，偶然发现的一件事情：

**当你跟 Agent 沟通时，大多数问题不是因为「不够精确」，而是因为「精确到了错误的地方」。**

把精确留给角色和关系，把自由留给自然语言。

契约够用就好。

---

*2025 · 写于持续和 Agent 磨合的过程中*
