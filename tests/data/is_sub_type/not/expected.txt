001 true  # single branch carries the refutation
002 false  # refutation under another tag
003 false  # slot holds the plain type
004 true  # refutation argument is a supertype
005 false  # slot holds an unrelated type
006 true  # sub is the same not[...]: reflexive
007 true  # refutation behind a name
008 true  # slot is never
009 true  # sub is never
010 true  # same tag plus an extra tag
011 true  # two branches, both carried
012 true  # two branches, order swapped
013 false  # first branch only
014 false  # first slot plain, second carried
015 false  # first carried, second plain
016 false  # both slots plain
017 true  # both carried plus an extra tag
018 true  # first argument is a supertype
019 false  # a tag the sup never names
020 true  # sub is the two-branch not[...]: reflexive
021 true  # first slot is never
022 true  # second slot is never
023 false  # slot holds a sum
024 true  # sub is never
025 true  # whole product behind a name
026 true  # half the product behind a name
027 false  # sub is the one-branch not[...]
028 true  # three branches, all carried
029 false  # three branches, one missing
030 false  # three branches, two missing
031 false  # three branches, last plain
032 true  # three branches plus an extra tag
033 false  # not[int] vs int
034 true  # not[int] vs never <- int
035 false  # not[$a P | int] vs one tagged slot
036 true  # not[$a P | int] vs never <- ($a P | int)
037 true  # not[Crimes], Crimes names a sum
038 true  # one branch behind a name
039 false  # not[not[$a P]]
040 false  # not with a second argument
041 true  # the not[...] itself behind a name
042 false  # not[int | str] vs not[int]
043 true  # not[int] vs not[int | str]: argument contravariance
044 true  # not nested one tag deep
045 false  # not nested one tag deep, branch missing
046 true  # not nested two tags deep
047 true  # two not[...] fields, both carried
048 false  # two not[...] fields, second branch missing
049 false  # two not[...] fields, first partial
050 true  # not over an inline branch type
051 true  # equivalence 1/2: not[A] <: never <- A, A = int
052 true  # equivalence 2/2: never <- A <: not[A], A = int
053 true  # equivalence 1/2: not[A] <: never <- A, A = str
054 true  # equivalence 2/2: never <- A <: not[A], A = str
055 true  # equivalence 1/2: not[A] <: never <- A, A = bool
056 true  # equivalence 2/2: never <- A <: not[A], A = bool
057 true  # equivalence 1/2: not[A] <: never <- A, A = int | str
058 true  # equivalence 2/2: never <- A <: not[A], A = int | str
059 true  # equivalence 1/2: not[A] <: never <- A, A = int | str | bool
060 true  # equivalence 2/2: never <- A <: not[A], A = int | str | bool
061 true  # equivalence 1/2: not[A] <: never <- A, A = $a int
062 true  # equivalence 2/2: never <- A <: not[A], A = $a int
063 true  # equivalence 1/2: not[A] <: never <- A, A = $a int | $b str
064 true  # equivalence 2/2: never <- A <: not[A], A = $a int | $b str
065 true  # equivalence 1/2: not[A] <: never <- A, A = $a int | $b str | $c bool
066 true  # equivalence 2/2: never <- A <: not[A], A = $a int | $b str | $c bool
067 true  # equivalence 1/2: not[A] <: never <- A, A = (int, str)
068 true  # equivalence 2/2: never <- A <: not[A], A = (int, str)
069 true  # equivalence 1/2: not[A] <: never <- A, A = $a int * $b str
070 true  # equivalence 2/2: never <- A <: not[A], A = $a int * $b str
071 true  # equivalence 1/2: not[A] <: never <- A, A = list[int]
072 true  # equivalence 2/2: never <- A <: not[A], A = list[int]
073 true  # equivalence 1/2: not[A] <: never <- A, A = dict[str, int]
074 true  # equivalence 2/2: never <- A <: not[A], A = dict[str, int]
075 true  # equivalence 1/2: not[A] <: never <- A, A = never
076 true  # equivalence 2/2: never <- A <: not[A], A = never
077 true  # equivalence 1/2: not[A] <: never <- A, A = nil
078 true  # equivalence 2/2: never <- A <: not[A], A = nil
079 true  # equivalence 1/2: not[A] <: never <- A, A = not[int]
080 true  # equivalence 2/2: never <- A <: not[A], A = not[int]
081 true  # equivalence 1/2: not[A] <: never <- A, A = never <- int
082 true  # equivalence 2/2: never <- A <: not[A], A = never <- int
083 true  # equivalence 1/2: not[A] <: never <- A, A = 'lit'
084 true  # equivalence 2/2: never <- A <: not[A], A = 'lit'
085 true  # equivalence 1/2: not[A] <: never <- A, A = 42
086 true  # equivalence 2/2: never <- A <: not[A], A = 42
087 true  # equivalence 1/2: not[A] <: never <- A, A = Ref
088 true  # equivalence 2/2: never <- A <: not[A], A = Ref
089 true  # equivalence 1/2: not[A] <: never <- A, A = NamedSum
090 true  # equivalence 2/2: never <- A <: not[A], A = NamedSum
091 true  # equivalence 1/2: not[A] <: never <- A, A = set[bool]
092 true  # equivalence 2/2: never <- A <: not[A], A = set[bool]
093 true  # equivalence 1/2: not[A] <: never <- A, A = int | never
094 true  # equivalence 2/2: never <- A <: not[A], A = int | never
095 true  # equivalence 1/2: not[A] <: never <- A, A = $a (int | str)
096 true  # equivalence 2/2: never <- A <: not[A], A = $a (int | str)
097 true  # equivalence 1/2: not[A] <: never <- A, A = list[$a int]
098 true  # equivalence 2/2: never <- A <: not[A], A = list[$a int]
099 true  # equivalence 1/2: not[A] <: never <- A, A = $a int * ($b str | $c bool)
100 true  # equivalence 2/2: never <- A <: not[A], A = $a int * ($b str | $c bool)
