# The first argument is what the member is taken from, and it is always the one
# written first — here `$sub_env_name "kid"`, which keeps no `$sub_env` member.
__ret__ = $sub_env << $sub_env_name "kid"
