take_x =
    int
  <- $env Environment
  <- $x CalledByNeed[int]
  <- { answer whatever x is }
tick =
    int <- $env Environment <- { counts as a side effect }
__ret__ =
    take_x
    << $env environ
    << $x (a := (tick << $env environ)  a)
