import let_inner as let_inner
add =
    int <- $env Environment <- $a int <- $b int <- { add the two }
A = (
  one := let_inner << (environ.tmp_sub_env << environ) << ()
  two := let_inner << (environ.tmp_sub_env << environ) << ()
  add << $env environ << $a one << $b two
)
__ret__ = A
