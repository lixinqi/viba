ask_twice =
    int
  <- $env Environment
  <- $x CalledByNeed[int]
  <- { add x to itself }
tick =
    int <- $env Environment <- { counts as a side effect }
__ret__ =
    ask_twice
    << $env environ
    << $x (a := (tick << $env environ)  a)
