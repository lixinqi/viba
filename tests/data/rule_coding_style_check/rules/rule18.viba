ParamCount := int
CyclomaticComplexity := int
CoverageRatio := float
IndentWidth := $spaces int
IsPublic := bool
ModuleName := str
Keywords := list[str]
BudgetSplit := $spent float * $cap float
AuthorName := str

Rule18 :=
  RuleObject
  * $param_count Metric[ParamCount]
  * $cyclomatic_complexity Metric[CyclomaticComplexity]
  * $coverage_ratio Metric[CoverageRatio]
  * $indent_width Metric[IndentWidth]
  * $is_public Metric[IsPublic]
  * $module_name Metric[ModuleName]
  * $keywords Metric[Keywords]
  * $budget_split Metric[BudgetSplit]
  * $author_name Metric[AuthorName]
  * $assert_00_param_count_at_most_24
      Predicate[{param_count at most 24}, $python_code {
def predicate(self):
    return self.get_param_count().get_value().value <= 24
}]
  * $assert_01_cyclomatic_complexity_at_most_24
      Predicate[{cyclomatic_complexity at most 24}, $python_code {
def predicate(self):
    return self.get_cyclomatic_complexity().get_value().value <= 24
}]
  * $assert_02_param_count_not_above_cyclomatic_complexity
      Predicate[{param_count not above cyclomatic_complexity}, $python_code {
def predicate(self):
    return self.get_param_count().get_value().value <= self.get_cyclomatic_complexity().get_value().value
}]
  * $assert_03_coverage_ratio_within_50_5
      Predicate[{coverage_ratio within 50.5}, $python_code {
def predicate(self):
    return self.get_coverage_ratio().get_value().value <= 50.5
}]
  * $assert_04_coverage_ratio_non_negative
      Predicate[{coverage_ratio non-negative}, $python_code {
def predicate(self):
    return self.get_coverage_ratio().get_value().value >= 0.0
}]
  * $assert_05_is_public_required
      Predicate[{is_public required}, $python_code {
def predicate(self):
    return self.get_is_public().get_value().value
}]
  * $assert_06_module_name_short
      Predicate[{module_name short}, $python_code {
def predicate(self):
    return len(self.get_module_name().get_value().value) <= 8
}]
  * $assert_07_module_name_not_empty
      Predicate[{module_name not empty}, $python_code {
def predicate(self):
    return len(self.get_module_name().get_value().value) >= 1
}]
  * $assert_08_author_name_short
      Predicate[{author_name short}, $python_code {
def predicate(self):
    return len(self.get_author_name().get_value().value) <= 8
}]
  * $assert_09_author_name_not_empty
      Predicate[{author_name not empty}, $python_code {
def predicate(self):
    return len(self.get_author_name().get_value().value) >= 1
}]
