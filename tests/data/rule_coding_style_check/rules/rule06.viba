CodeLength := int
IndentWidth := $spaces int
DocCoverage := $documented_lines int * $total_lines int
BudgetSplit := $spent float * $cap float
HasTests := bool
Keywords := list[str]
AuthorName := str

Rule06 :=
  RuleObject
  * $code_length Metric[CodeLength]
  * $indent_width Metric[IndentWidth]
  * $doc_coverage Metric[DocCoverage]
  * $budget_split Metric[BudgetSplit]
  * $has_tests Metric[HasTests]
  * $keywords Metric[Keywords]
  * $author_name Metric[AuthorName]
  * $assert_00_code_length_at_most_24
      Predicate[{code_length at most 24}, $python_code {
def predicate(self):
    return self.code_length.value <= 24
}]
  * $assert_01_has_tests_required
      Predicate[{has_tests required}, $python_code {
def predicate(self):
    return self.has_tests.value
}]
  * $assert_02_author_name_short
      Predicate[{author_name short}, $python_code {
def predicate(self):
    return len(self.author_name.value) <= 8
}]
  * $assert_03_author_name_not_empty
      Predicate[{author_name not empty}, $python_code {
def predicate(self):
    return len(self.author_name.value) >= 1
}]
  * $assert_04_keywords_non_empty
      Predicate[{keywords non-empty}, $python_code {
def predicate(self):
    return len(self.keywords.value) >= 1
}]
  * $assert_05_keywords_bounded
      Predicate[{keywords bounded}, $python_code {
def predicate(self):
    return len(self.keywords.value) <= 4
}]
  * $assert_06_indent_width_non_negative
      Predicate[{indent_width non-negative}, $python_code {
def predicate(self):
    return self.indent_width.spaces >= 0
}]
  * $assert_07_doc_coverage_documented_within_total
      Predicate[{doc_coverage documented within total}, $python_code {
def predicate(self):
    return self.doc_coverage.documented_lines <= self.doc_coverage.total_lines
}]
  * $assert_08_budget_split_spent_within_cap
      Predicate[{budget_split spent within cap}, $python_code {
def predicate(self):
    return self.budget_split.spent <= self.budget_split.cap
}]
