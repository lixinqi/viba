# rule15.viba 的一份 witness：seed=15, fail_prob=0.1
# verdict：Ok(True)
Instance15 :=
    RuleObject
    * $code_length 26
    * $loop_count 1
    * $param_count 66
    * $coverage_ratio 73.6
    * $has_tests true
    * $module_name "beta"
    * $scores DictLiteral
    * $imports ListLiteral
    * $assert_00_code_length_at_most_24 Predicate[{code_length at most 24}, $python_code {
def predicate(self):
    return self.code_length.value <= 24
}]
    * $assert_01_code_length_under_100 Predicate[{code_length under 100}, $python_code {
def predicate(self):
    return self.code_length.value < 100
}]
    * $assert_02_loop_count_at_most_24 Predicate[{loop_count at most 24}, $python_code {
def predicate(self):
    return self.loop_count.value <= 24
}]
    * $assert_03_loop_count_under_100 Predicate[{loop_count under 100}, $python_code {
def predicate(self):
    return self.loop_count.value < 100
}]
    * $assert_04_param_count_at_most_24 Predicate[{param_count at most 24}, $python_code {
def predicate(self):
    return self.param_count.value <= 24
}]
    * $assert_05_param_count_under_100 Predicate[{param_count under 100}, $python_code {
def predicate(self):
    return self.param_count.value < 100
}]
    * $assert_06_code_length_not_above_loop_count Predicate[{code_length not above loop_count}, $python_code {
def predicate(self):
    return self.code_length.value <= self.loop_count.value
}]
    * $assert_07_coverage_ratio_within_50_5 Predicate[{coverage_ratio within 50.5}, $python_code {
def predicate(self):
    return self.coverage_ratio.value <= 50.5
}]
    * $assert_08_coverage_ratio_non_negative Predicate[{coverage_ratio non-negative}, $python_code {
def predicate(self):
    return self.coverage_ratio.value >= 0.0
}]
    * $assert_09_has_tests_required Predicate[{has_tests required}, $python_code {
def predicate(self):
    return self.has_tests.value
}]
