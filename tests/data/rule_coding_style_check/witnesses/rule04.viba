# rule04.viba 的一份 witness：seed=04, fail_prob=0.1
# verdict：Ok(False)
Instance04 :=
    RuleObject
    * $loop_count 30
    * $max_lines 38
    * $time_budget 10.32
    * $has_tests true
    * $scores DictLiteral
    * $assert_00_loop_count_at_most_24 PredicationFailed[nil, str]
    * $assert_01_loop_count_under_100 PredicationFailed[nil, str]
    * $assert_02_max_lines_at_most_24 Predicate[{max_lines at most 24}, $python_code {
def predicate(self):
    return self.max_lines.value <= 24
}]
    * $assert_03_max_lines_under_100 Predicate[{max_lines under 100}, $python_code {
def predicate(self):
    return self.max_lines.value < 100
}]
    * $assert_04_loop_count_not_above_max_lines Predicate[{loop_count not above max_lines}, $python_code {
def predicate(self):
    return self.loop_count.value <= self.max_lines.value
}]
    * $assert_05_time_budget_within_50_5 PredicationFailed[nil, str]
    * $assert_06_time_budget_non_negative Predicate[{time_budget non-negative}, $python_code {
def predicate(self):
    return self.time_budget.value >= 0.0
}]
    * $assert_07_has_tests_required Predicate[{has_tests required}, $python_code {
def predicate(self):
    return self.has_tests.value
}]
    * $assert_08_has_tests_forbidden Predicate[{has_tests forbidden}, $python_code {
def predicate(self):
    return not self.has_tests.value
}]
    * $assert_09_scores_non_empty Predicate[{scores non-empty}, $python_code {
def predicate(self):
    return len(self.scores.value) >= 1
}]
