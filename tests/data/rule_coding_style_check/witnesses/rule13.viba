# rule13.viba 的一份 witness：seed=13, fail_prob=0.1
# verdict：Ok(False)
Instance13 :=
    RuleObject
    * $code_length 33
    * $cyclomatic_complexity 37
    * $coverage_ratio 68.53
    * $budget_split(    $spent 68.41
      * $cap 84.93)
    * $module_name "beta"
    * $imports ListLiteral["beta", "beta"]
    * $is_public false
    * $assert_00_code_length_at_most_24 Predicate[{code_length at most 24}, $python_code {
def predicate(self):
    return self.code_length.value <= 24
}]
    * $assert_01_code_length_under_100 Predicate[{code_length under 100}, $python_code {
def predicate(self):
    return self.code_length.value < 100
}]
    * $assert_02_cyclomatic_complexity_at_most_24 PredicationFailed[nil, str]
    * $assert_03_cyclomatic_complexity_under_100 Predicate[{cyclomatic_complexity under 100}, $python_code {
def predicate(self):
    return self.cyclomatic_complexity.value < 100
}]
    * $assert_04_code_length_not_above_cyclomatic_complexity Predicate[{code_length not above cyclomatic_complexity}, $python_code {
def predicate(self):
    return self.code_length.value <= self.cyclomatic_complexity.value
}]
    * $assert_05_coverage_ratio_within_50_5 PredicationFailed[nil, str]
    * $assert_06_coverage_ratio_non_negative Predicate[{coverage_ratio non-negative}, $python_code {
def predicate(self):
    return self.coverage_ratio.value >= 0.0
}]
    * $assert_07_is_public_required Predicate[{is_public required}, $python_code {
def predicate(self):
    return self.is_public.value
}]
    * $assert_08_is_public_forbidden Predicate[{is_public forbidden}, $python_code {
def predicate(self):
    return not self.is_public.value
}]
    * $assert_09_module_name_short Predicate[{module_name short}, $python_code {
def predicate(self):
    return len(self.module_name.value) <= 8
}]
