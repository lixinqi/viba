import branch
__ret__ =
    Oneof
  | (
      branch.id_or_never
      << $env environ
      << $condition (c := true  c)
      << $v (c)
    )
  | (
      branch.never_or_id
      << $env environ
      << $condition false
      << $v (0)
    )
