import branch
A = (
  bonus := 7
  Oneof
  | (
      branch.id_or_never
      << $env environ
      << $condition true
      << $v (bonus)
    )
  | (
      branch.never_or_id
      << $env environ
      << $condition true
      << $v (0)
    )
)
__ret__ = A
