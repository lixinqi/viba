# rule19.viba 的一份 witness：seed=19, fail_prob=0.1
# verdict：Ok(False)
Instance19 :=
    RuleObject
    * $code_length 86
    * $max_lines 5
    * $loop_count 100
    * $param_count 66
    * $coverage_ratio 12.07
    * $time_budget 19.95
    * $has_tests true
    * $is_public false
    * $module_name "beta"
    * $author_name "gamma"
    * $assert_00_code_length_at_most_24 Predicate[{code_length at most 24}, $python_code {
def predicate(self):
    return self.code_length.value <= 24
}]
    * $assert_01_code_length_under_100 Predicate[{code_length under 100}, $python_code {
def predicate(self):
    return self.code_length.value < 100
}]
    * $assert_02_max_lines_at_most_24 Predicate[{max_lines at most 24}, $python_code {
def predicate(self):
    return self.max_lines.value <= 24
}]
    * $assert_03_max_lines_under_100 Predicate[{max_lines under 100}, $python_code {
def predicate(self):
    return self.max_lines.value < 100
}]
    * $assert_04_loop_count_at_most_24 Predicate[{loop_count at most 24}, $python_code {
def predicate(self):
    return self.loop_count.value <= 24
}]
    * $assert_05_loop_count_under_100 Predicate[{loop_count under 100}, $python_code {
def predicate(self):
    return self.loop_count.value < 100
}]
    * $assert_06_param_count_at_most_24 PredicationFailed[nil, str]
    * $assert_07_param_count_under_100 Predicate[{param_count under 100}, $python_code {
def predicate(self):
    return self.param_count.value < 100
}]
    * $assert_08_code_length_not_above_max_lines Predicate[{code_length not above max_lines}, $python_code {
def predicate(self):
    return self.code_length.value <= self.max_lines.value
}]
    * $assert_09_coverage_ratio_within_50_5 Predicate[{coverage_ratio within 50.5}, $python_code {
def predicate(self):
    return self.coverage_ratio.value <= 50.5
}]
