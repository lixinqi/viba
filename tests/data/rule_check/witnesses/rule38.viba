# rule38.viba 的一份 witness：seed=38, fail_prob=0.1
# verdict：Ok(False)
Instance38 :=
    RuleObject
    * $code_length 81
    * $max_lines 53
    * $loop_count 54
    * $coverage_ratio 75.45
    * $time_budget 10.16
    * $has_tests true
    * $author_name "delta"
    * $keywords ListLiteral["alpha"]
    * $int_list ListLiteral[85, 21]
    * $int_pair(    $a 75
      * $b 44)
    * $doc_coverage(    $documented_lines 41
      * $total_lines 34)
    * $assert_00_code_length_at_most_24 Predicate[{code_length at most 24}, $python_code {
def predicate(self):
    return self.code_length.value <= 24
}]
    * $assert_01_code_length_under_100 Predicate[{code_length under 100}, $python_code {
def predicate(self):
    return self.code_length.value < 100
}]
    * $assert_02_max_lines_at_most_24 Predicate[{max_lines at most 24}, $python_code {
def predicate(self):
    return self.max_lines.value <= 24
}]
    * $assert_03_max_lines_under_100 Predicate[{max_lines under 100}, $python_code {
def predicate(self):
    return self.max_lines.value < 100
}]
    * $assert_04_loop_count_at_most_24 Predicate[{loop_count at most 24}, $python_code {
def predicate(self):
    return self.loop_count.value <= 24
}]
    * $assert_05_loop_count_under_100 Predicate[{loop_count under 100}, $python_code {
def predicate(self):
    return self.loop_count.value < 100
}]
    * $assert_06_code_length_not_above_max_lines Predicate[{code_length not above max_lines}, $python_code {
def predicate(self):
    return self.code_length.value <= self.max_lines.value
}]
    * $assert_07_coverage_ratio_within_50_5 PredicationFailed[nil, str]
    * $assert_08_coverage_ratio_non_negative Predicate[{coverage_ratio non-negative}, $python_code {
def predicate(self):
    return self.coverage_ratio.value >= 0.0
}]
    * $assert_09_time_budget_within_50_5 Predicate[{time_budget within 50.5}, $python_code {
def predicate(self):
    return self.time_budget.value <= 50.5
}]
    * $assert_10_time_budget_non_negative Predicate[{time_budget non-negative}, $python_code {
def predicate(self):
    return self.time_budget.value >= 0.0
}]
    * $assert_11_has_tests_required Predicate[{has_tests required}, $python_code {
def predicate(self):
    return self.has_tests.value
}]
    * $assert_12_has_tests_forbidden Predicate[{has_tests forbidden}, $python_code {
def predicate(self):
    return not self.has_tests.value
}]
    * $assert_13_author_name_short Predicate[{author_name short}, $python_code {
def predicate(self):
    return len(self.author_name.value) <= 8
}]
    * $assert_14_author_name_not_empty Predicate[{author_name not empty}, $python_code {
def predicate(self):
    return len(self.author_name.value) >= 1
}]
    * $assert_15_keywords_non_empty Predicate[{keywords non-empty}, $python_code {
def predicate(self):
    return len(self.keywords.value) >= 1
}]
    * $assert_16_keywords_bounded Predicate[{keywords bounded}, $python_code {
def predicate(self):
    return len(self.keywords.value) <= 4
}]
    * $assert_17_doc_coverage_documented_within_total Predicate[{doc_coverage documented within total}, $python_code {
def predicate(self):
    return self.doc_coverage.documented_lines <= self.doc_coverage.total_lines
}]
