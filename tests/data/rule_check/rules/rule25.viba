CodeLength := int
MaxLines := int
CoverageRatio := float
TimeBudget := float
HasTests := bool
ModuleName := str
Keywords := list[str]
Tags := set[str]
Scores := dict[str, int]
Lookup := dict[str, str]
IntPair := $a int * $b int
NameSize := $name str * $size int
Triplet := $x int * $y int * $z int
DocCoverage := $documented_lines int * $total_lines int

Rule25 :=
  RuleObject
  * $code_length Metric[CodeLength]
  * $max_lines Metric[MaxLines]
  * $coverage_ratio Metric[CoverageRatio]
  * $time_budget Metric[TimeBudget]
  * $has_tests Metric[HasTests]
  * $module_name Metric[ModuleName]
  * $keywords Metric[Keywords]
  * $tags Metric[Tags]
  * $scores Metric[Scores]
  * $lookup Metric[Lookup]
  * $int_pair Metric[IntPair]
  * $name_size Metric[NameSize]
  * $triplet Metric[Triplet]
  * $doc_coverage Metric[DocCoverage]
  * $assert_00_code_length_at_most_24
      Predicate[{code_length at most 24}, $python_code {
def predicate(self):
    return self.code_length.value <= 24
}]
  * $assert_01_code_length_under_100
      Predicate[{code_length under 100}, $python_code {
def predicate(self):
    return self.code_length.value < 100
}]
  * $assert_02_max_lines_at_most_24
      Predicate[{max_lines at most 24}, $python_code {
def predicate(self):
    return self.max_lines.value <= 24
}]
  * $assert_03_max_lines_under_100
      Predicate[{max_lines under 100}, $python_code {
def predicate(self):
    return self.max_lines.value < 100
}]
  * $assert_04_code_length_not_above_max_lines
      Predicate[{code_length not above max_lines}, $python_code {
def predicate(self):
    return self.code_length.value <= self.max_lines.value
}]
  * $assert_05_coverage_ratio_within_50_5
      Predicate[{coverage_ratio within 50.5}, $python_code {
def predicate(self):
    return self.coverage_ratio.value <= 50.5
}]
  * $assert_06_coverage_ratio_non_negative
      Predicate[{coverage_ratio non-negative}, $python_code {
def predicate(self):
    return self.coverage_ratio.value >= 0.0
}]
  * $assert_07_time_budget_within_50_5
      Predicate[{time_budget within 50.5}, $python_code {
def predicate(self):
    return self.time_budget.value <= 50.5
}]
  * $assert_08_time_budget_non_negative
      Predicate[{time_budget non-negative}, $python_code {
def predicate(self):
    return self.time_budget.value >= 0.0
}]
  * $assert_09_has_tests_required
      Predicate[{has_tests required}, $python_code {
def predicate(self):
    return self.has_tests.value
}]
  * $assert_10_has_tests_forbidden
      Predicate[{has_tests forbidden}, $python_code {
def predicate(self):
    return not self.has_tests.value
}]
  * $assert_11_module_name_short
      Predicate[{module_name short}, $python_code {
def predicate(self):
    return len(self.module_name.value) <= 8
}]
  * $assert_12_module_name_not_empty
      Predicate[{module_name not empty}, $python_code {
def predicate(self):
    return len(self.module_name.value) >= 1
}]
  * $assert_13_keywords_non_empty
      Predicate[{keywords non-empty}, $python_code {
def predicate(self):
    return len(self.keywords.value) >= 1
}]
  * $assert_14_keywords_bounded
      Predicate[{keywords bounded}, $python_code {
def predicate(self):
    return len(self.keywords.value) <= 4
}]
  * $assert_15_tags_bounded
      Predicate[{tags bounded}, $python_code {
def predicate(self):
    return len(self.tags.value) <= 3
}]
  * $assert_16_tags_non_empty
      Predicate[{tags non-empty}, $python_code {
def predicate(self):
    return len(self.tags.value) >= 1
}]
  * $assert_17_scores_non_empty
      Predicate[{scores non-empty}, $python_code {
def predicate(self):
    return len(self.scores.value) >= 1
}]
  * $assert_18_scores_bounded
      Predicate[{scores bounded}, $python_code {
def predicate(self):
    return len(self.scores.value) <= 4
}]
  * $assert_19_doc_coverage_documented_within_total
      Predicate[{doc_coverage documented within total}, $python_code {
def predicate(self):
    return self.doc_coverage.documented_lines <= self.doc_coverage.total_lines
}]
  * $assert_20_lookup_non_empty
      Predicate[{lookup non-empty}, $python_code {
def predicate(self):
    return len(self.lookup.value) >= 1
}]
  * $assert_21_lookup_bounded
      Predicate[{lookup bounded}, $python_code {
def predicate(self):
    return len(self.lookup.value) <= 4
}]
  * $assert_22_int_pair_ordered
      Predicate[{int_pair ordered}, $python_code {
def predicate(self):
    return self.int_pair.a <= self.int_pair.b
}]
  * $assert_23_int_pair_sum_bounded
      Predicate[{int_pair sum bounded}, $python_code {
def predicate(self):
    return self.int_pair.a + self.int_pair.b <= 100
}]
