# corpus case 013 — generated by generate_corpus.py (seed 20260915)
# themes: Report, Batch, Result, Check, Rule

Batch1[E, V] :=
    $input (
        '''a
b
c'''
            * 'strict'
    )

Result2[In, U] :=
    $src (
        GraphModule
            * 601
    )

Option3 :=
    (
        Error
            <- 'viba slow'
            <- {x + 1}
            <- {fn(fn(x + 1))}
    )
        <- (
            {fn(x + 1)}
                * 5.638
                * Error
                * '''keep
  indent'''
        )

Result5[S, U] :=
    $cfg (
        (
            ...
                <- 'slow'
        )
            <- (
                GraphModule
                    * void
            )
            <- (
                $res.val
                    * '''keep
  indent'''
                    * never
            )
    )

Task6[K, Out] :=
    (
        (
            Beta
                <- 'viba fast'
        )
            | (
                ...
                    * $cfg.mode
            )
            | 'lazy strict slow'
            | never
            | {x + 1}
            | 9.5
            | false
            | Beta
            | ...
    )
        <- (
            (
                1.109
                    <- 1.084
            )
                * Gamma
                * torch.Tensor
                * Config
                * (
                    Map['''a
b
c''']
                )
        )

List7 :=
    (
        Map[never]
    )
        * (
            List[false]
        )

Task8 :=
    $dst (
        {fn(fn(x + 1))}
            * false
            * ...
    )

Handler9[E, K] :=
    (
        $data '''keep
  indent'''
    )
        * (
            'strict'
                <- '''line one
line two'''
        )
        * (
            never
                <- ...
        )
        * 976
        * 'strict slow lazy'

Guarded10 :=
    $x float
    * Assert[{x <= 132}]
    * Error
