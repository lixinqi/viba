# corpus case 047 — generated by generate_corpus.py (seed 20260915)
# themes: Plan, Rule, Graph, Edge, Config

Graph1[T, Out] :=
    $res (
        (
            {fn(x + 1)}
        )
    )

Task2[E] :=
    (
        (
            'strict slow viba'
                <- Alpha
                <- {fn(fn(x + 1))}
        )
    )
        * (
            (
                Map['''a
b
c''']
            )
                | (
                    '''keep
  indent'''
                        * Config
                )
        )
        * (
            $next (
                never
                    * Error
                    * Handler
                    * 'strict'
            )
        )
        * (
            (
                (
                    'fast slow lazy'
                )
            )
                <- (
                    Context
                        * 'slow strict auto'
                        * never
                        * 'viba fast'
                )
                <- (
                    9.55
                        | 442
                        | Input
                )
                <- (
                    never
                        <- void
                        <- $env.scope ()
                        <- fx.Graph
                )
        )

Slice3[U, E] :=
    $meta (
        $name (
            $res 4.44
        )
    )

Slice4[E] :=
    (
        {x + 1}
            | 'strict'
            | '''line one
line two'''
    )

Batch6[In, Out] :=
    (
        (
            Beta
                | GraphModule
        )
            * Config
            * Error
    )

Option7[V] :=
    (
        Alpha
            * Gamma
            * ...
            * ...
    )
        | (
            Option[true, void]
        )

Pair8[Out] :=
    $value (
        Error
            * GraphModule
    )

Batch10[Out] :=
    List[void]

Edge13[T, In] :=
    $res true

Guarded17 :=
    $lines int
    * Assert[{lines <= 13}]
    * State
