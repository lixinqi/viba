# corpus case 120 — generated by generate_corpus.py (seed 20260915)
# themes: Map, Rule, Result, Stream, State

Config1[E, V] :=
    (
        Gamma
            | void
    )
        <- (
            ...
                | Context
                | true
        )
        <- (
            true
                | '''line one
line two'''
                | GraphModule
        )
        <- (
            (
                GraphModule
            )
        )

Report2 :=
    $next (
        (
            (
                Metric
                    | '''line one
line two'''
                    | {x + 1}
            )
                <- (
                    Delta
                        * $meta.id ()
                        * 'slow lazy strict'
                )
                <- (
                    Delta
                        * MatchResult
                        * 850
                        * {fn(x + 1)}
                )
                <- (
                    List[Omega, $env.scope (), true]
                )
        )
            * 351
            * 'fast'
            * (
                (
                    'strict slow'
                )
            )
            * (
                (
                    440
                )
            )
            * (
                $meta never
            )
            * {x + 1}
            * $cfg.mode ()
            * '''a
b
c'''
            * (
                (
                    void
                )
            )
            * '''line one
line two'''
            * '''keep
  indent'''
            * $a.b.c.d ()
            * 0.706
    )

Edge3[In] :=
    (
        $res never
    )
        | (
            true
                <- 0.75
                <- '''keep
  indent'''
        )

Rule5[T] :=
    Box[
        Result[{fn(fn(x + 1))}],
        torch.Tensor
            <- 'fast'
            <- {fn(x + 1)}
            <- {fn(x + 1)}
    ]

Tensor8 :=
    (
        {fn(x + 1)}
            * true
            * GraphModule
    )
        | (
            '''line one
line two'''
                * {fn(fn(x + 1))}
                * {x + 1}
                * torch.Tensor
        )
        | 9.0
        | {x + 1}
        | '''line one
line two'''
        | false
