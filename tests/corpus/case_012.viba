# corpus case 012 — generated by generate_corpus.py (seed 20260915)
# themes: Slice, Task, Plan, Tensor, Option

Check2[In] :=
    (
        (
            nil,
            torch.Tensor,
            $meta.id (),
            81
        )
    )
        | (
            Option[Gamma]
        )
        | (
            (
                $input true,
                List['fast', 4.17],
                Map[460, 'slow strict auto']
            )
        )

Plan3[Out, T] :=
    Map[
        $kind (
            false
                | 'auto slow viba'
                | 'viba'
        ),
        $next (
            List[nil, false]
        )
    ]

Config4[U] :=
    (
        (
            (
                5.8
            )
        )
            * (
                Input
                    | false
                    | never
            )
            * {fn(x + 1)}
            * true
            * (
                $data '''line one
line two'''
            )
    )

Graph5 :=
    (
        {x + 1}
            * '''a
b
c'''
            * 1.85
    )
        | (
            Error
                <- 277
                <- Handler
                <- 'fast auto'
        )
        | (
            MatchResult
                <- Gamma
        )

Check7[In] :=
    (
        $next {fn(x + 1)},
        '''keep
  indent'''
            <- never,
        (
            Alpha,
            'fast',
            torch.Tensor,
            4.543
        )
    )

Tensor8[In, U] :=
    (
        359
            * 3.146
            * 261
            * Handler
    )
        <- (
            false
                * {x + 1}
        )

Guarded9 :=
    $ratio float
    * Assert[{ratio <= 48}]
    * State
