# corpus case 110 — generated by generate_corpus.py (seed 20260915)
# themes: Edge, Map, Slice, Option, Report

Config1[Out] :=
    $src (
        'auto lazy slow'
            * 'lazy'
            * $cfg.mode ()
    )

Rule2 :=
    Result[
        {fn(x + 1)}
            * torch.Tensor,
        252
            | 'slow lazy'
            | 'strict'
            | false,
        Config
            <- {fn(fn(x + 1))}
    ]

Handler3[S] :=
    Map[
        $input false,
        $input 'viba fast',
        Result['viba strict']
    ]

Config4[S] :=
    (
        (
            true
                * fx.Graph
        )
            | {fn(fn(x + 1))}
            | never
            | MatchResult
            | torch.Tensor
            | 7.44
            | 6.39
            | Metric
            | 962
    )
        * (
            (
                Box[8.85, ..., void]
            )
                <- (
                    Map['lazy']
                )
                <- (
                    Output
                        | GraphModule
                )
        )
        * (
            (
                (
                    ...
                )
            )
                <- (
                    Omega
                        * GraphModule
                )
                <- (
                    Output
                        * $res.val ()
                )
                <- (
                    $cfg 2.6
                )
        )
        * (
            (
                9.73
                    * 748
                    * $env.scope ()
            )
                <- (
                    false
                        * GraphModule
                )
                <- (
                    $kind true
                )
                <- (
                    'lazy strict'
                        * GraphModule
                        * {fn(x + 1)}
                )
        )

Plan6[Out, K] :=
    $src (
        (
            {x + 1}
                <- torch.Tensor
                <- void
        )
            | (
                $kind {fn(x + 1)}
            )
    )

Guarded7 :=
    $lines int
    * Assert[{lines <= 201}]
    * MatchResult
