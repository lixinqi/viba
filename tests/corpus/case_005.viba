# corpus case 005 — generated by generate_corpus.py (seed 20260915)
# themes: Task, Plan, Option, Edge, Rule

Config1[U, In] :=
    (
        (
            $meta Beta
        )
            <- (
                $cfg void
            )
    )

Config2[U, Out] :=
    (
        (
            8.608
        )
    )
        | (
            List[Handler, Gamma]
        )
        | (
            Option['strict viba']
        )

Node3[Out] :=
    List[
        Map[
            torch.Tensor
                * ...
                * never,
            $name {fn(x + 1)}
        ],
        (
            Pair['slow', $cfg.mode ()]
        )
            * 1.8
            * Delta
            * 6.93
            * (
                $err 'viba slow'
            )
            * (
                Map[721, Gamma]
            )
    ]

Task5[S] :=
    (
        $output (
            Map['''a
b
c''', {fn(x + 1)}, 'fast strict']
        )
    )
        * (
            List[
                'lazy'
                    <- '''line one
line two'''
                    <- never,
                'fast slow'
                    | MatchResult
            ]
        )

Pair6[U, Out] :=
    (
        ...
            <- never
            <- 808
    )
        <- (
            {fn(fn(x + 1))}
                <- true
                <- false
        )
        <- (
            $data 7.894
        )

Config7 :=
    '''keep
  indent'''
        * Output
        * ...
        * (
            $output 0.311
        )
        * $res.val ()
        * Beta
        * $res.val ()

Config9 :=
    List[
        466
            <- never
            <- 'lazy viba'
    ]

Option13 :=
    $err 'viba slow'

Guarded14 :=
    $ratio int
    * Assert[{ratio <= 364}]
    * Delta
