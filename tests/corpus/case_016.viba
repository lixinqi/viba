# corpus case 016 — generated by generate_corpus.py (seed 20260915)
# themes: Slice, Rule, Config, Task, Stream

Report1 :=
    (
        (
            3.1
                * Input
                * true
                * 819
        )
            | (
                Result['''keep
  indent''', 143]
            )
            | (
                Omega
                    * torch.Tensor
                    * void
            )
            | true
            | 973
            | 'viba'
            | MatchResult
    )

Config3 :=
    (
        '''a
b
c'''
            * 'strict slow auto'
            * 'strict'
            * Handler
            * 'lazy'
            * Gamma
    )

Node5 :=
    (
        (
            Metric
                | never
        )
            <- (
                'fast lazy'
                    | 54
            )
            <- (
                'slow fast viba'
                    * {fn(fn(x + 1))}
                    * fx.Graph
            )
    )
        * (
            $name true
        )
        * 'auto strict viba'
        * 'slow'
        * (
            'auto fast'
                <- {x + 1}
        )
        * MatchResult
        * 'lazy viba'
        * (
            489
                | $meta.id ()
                | Sigma
                | 5.6
        )
        * 'auto viba fast'
        * true
        * false
        * (
            Option[Sigma]
        )
        * (
            never
                | true
                | (
                    $kind {fn(fn(x + 1))}
                )
                | (
                    void
                        * Omega
                        * Beta
                )
                | never
                | Metric
                | 6.1
                | fx.Graph
        )

Result6[K] :=
    never
        * $cfg.mode ()
        * true
        * Input
        * {fn(fn(x + 1))}
        * (
            Omega
                | Handler
                | 645
                | 'fast slow strict'
        )
        * true
        * 3.4

Guarded7 :=
    $ratio int
    * Assert[{ratio <= 427}]
    * Handler
