# corpus case 090 — generated by generate_corpus.py (seed 20260915)
# themes: Report, Config, Map, Tensor, Option

Result1 :=
    (
        (
            $cfg void
        )
    )
        * (
            $src (
                364
                    * '''line one
line two'''
                    * never
                    * 0.8
            )
        )

Check2[K] :=
    '''line one
line two'''
        * 0.2
        * (
            (
                Alpha
            )
        )
        * (
            false
                | '''line one
line two'''
                | never
        )
        * (
            $name void
        )
        * (
            $idx (
                (
                    'viba strict slow'
                )
            )
        )
        * (
            {fn(x + 1)}
                | true
                | 571
                | 'fast lazy viba'
                | (
                    '''line one
line two'''
                        * fx.Graph
                        * void
                )
        )

Report4 :=
    Option[
        Map[
            $res 337,
            List[MatchResult, GraphModule]
        ],
        $dst (
            (
                $env.scope
            )
        ),
        (
            true
                <- torch.Tensor
                <- never
                <- Context
        )
    ]

Map5[Out, In] :=
    (
        Output
            <- ...
    )
        * (
            Map[7.982]
        )

Pair6 :=
    (
        List[
            Input
                * torch.Tensor
                * Beta
                * 731,
            Option[0.1, $cfg.mode, 'fast'],
            $env.scope
                <- ...
                <- never
        ]
    )

Edge8[In, T] :=
    $res (
        $cfg.mode
            * Input
            * ...
    )

Guarded9 :=
    $ratio int
    * Assert[{ratio <= 18}]
    * Sigma
