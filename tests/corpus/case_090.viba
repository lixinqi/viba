# corpus case 090 — generated by generate_corpus.py (seed 20260915)
# themes: Graph, Handler, Rule, Pair, Map

Handler2[V] =
    $idx (
        $next (
            ...
                * 901
                * never
                * 830
        )
    )

Map3 =
    (
        Omega
            | Beta
            | false
            | Beta
            | torch.Tensor
            | nil
            | Error
            | (
                $cfg.mode ()
                    <- 'slow lazy'
            )
            | (
                $res.val ()
                    * 'lazy slow strict'
                    * Error
            )
    )
        <- (
            $meta (
                {fn(x + 1)}
                    * nil
                    * Error
            )
        )
        <- (
            (
                (
                    Alpha
                )
            )
                | (
                    false
                        <- 'lazy'
                        <- $cfg.mode ()
                        <- Beta
                )
                | (
                    Beta
                        * '''a
b
c'''
                        * never
                        * 'viba'
                )
        )
        <- (
            (
                ...
                    | nil
            )
                <- (
                    769
                        <- never
                        <- false
                        <- $cfg.mode ()
                )
                <- (
                    Delta
                        | 'fast slow strict'
                )
                <- (
                    (
                        nil,
                        Context
                    )
                )
        )

Tensor5[V] =
    (
        GraphModule
            * State
            * 'lazy auto strict'
    )

State6[V, U] =
    Map[
        'lazy viba strict'
            * Context,
        'slow viba'
            <- {fn(x + 1)}
            <- nil,
        '''keep
  indent'''
            * torch.Tensor
            * Omega
    ]

Batch8[In] =
    (
        '''keep
  indent'''
            <- Input
    )

Guarded9 =
    $lines float
    * Assert[{lines <= 411}]
    * fx.Graph
