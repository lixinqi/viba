# corpus case 084 — generated by generate_corpus.py (seed 20260915)
# themes: Graph, Tensor, State, Config, Stream

Node1[T, S] :=
    Result[
        $res (
            (
                Output
            )
        ),
        $dst (
            ...
                <- 'strict'
        ),
        (
            Result[State, Sigma, never]
        )
            * (
                $name 8.59
            )
            * (
                (
                    Config
                )
            )
    ]

Handler2 :=
    (
        (
            (
                4.61
            )
        )
    )

Check3 :=
    Error
        * 0.1
        * (
            'auto'
                | '''a
b
c'''
        )
        * (
            8.22
                | true
        )

Edge4[V] :=
    Result[
        never
            * State,
        '''line one
line two'''
            * 3.91
            * GraphModule,
        Result['lazy slow', void]
    ]

Check6[K, S] :=
    Option[
        List[
            {x + 1}
                * {fn(x + 1)}
                * true
                * Delta
        ],
        $src (
            (
                {x + 1}
            )
        )
    ]

Slice7[In, S] :=
    $dst (
        Metric
            | 733
            | '''line one
line two'''
            | (
                State
                    <- Delta
                    <- 8.67
                    <- Sigma
            )
            | (
                never
                    * Handler
            )
    )

Handler8[S] :=
    Delta
        * GraphModule
        * (
            $res '''line one
line two'''
        )
        * (
            Pair[{fn(fn(x + 1))}]
        )

Guarded9 :=
    $x int
    * Assert[{x <= 404}]
    * Sigma
