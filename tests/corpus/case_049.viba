# corpus case 049 — generated by generate_corpus.py (seed 20260915)
# themes: Rule, Tensor, Report, Config, Plan

Rule1[U] :=
    Box[
        $idx (
            (
                void
                    * '''line one
line two'''
                    * MatchResult
                    * never
            )
                <- (
                    false
                        <- 'viba slow'
                )
                <- (
                    {fn(fn(x + 1))}
                        * Omega
                        * 8.5
                        * 'slow'
                )
                <- (
                    {fn(x + 1)}
                        * MatchResult
                )
        )
    ]

Batch3 :=
    (
        (
            ...
                <- {x + 1}
        )
            <- (
                ...
                    <- $meta.id
                    <- 'strict'
            )
            <- (
                Map[0.9, 487, fx.Graph]
            )
            <- (
                $input 5.91
            )
    )
        | (
            (
                Option[MatchResult, 'viba strict lazy']
            )
                * (
                    Result[Error, never, 363]
                )
                * (
                    $next true
                )
                * 1.29
                * {fn(x + 1)}
                * 580
                * 'lazy'
        )

Stream4[V] :=
    (
        'auto'
            | '''line one
line two'''
            | (
                $env.scope
                    * {fn(x + 1)}
                    * Delta
                    * (
                        $res.val Unit
                    )
            )
    )

Slice5 :=
    (
        $dst $meta.id
    )
        * 'fast'
        * Error
        * '''keep
  indent'''
        * '''line one
line two'''
        * 'auto viba'
        * Context
        * 'fast viba'
        * Sigma

Plan8[T, S] :=
    $res (
        MatchResult
            | 'fast slow lazy'
            | 390
    )

Option10[T] :=
    Map[7.487, Sigma, void]

Tensor14[In] :=
    $output never

Option17 :=
    never
        * 4.3
        * false
        * 591
