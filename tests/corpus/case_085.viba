# corpus case 085 — generated by generate_corpus.py (seed 20260915)
# themes: Result, Node, Map, Edge, Stream

Map1[T, S] =
    (
        List[
            Map[
                (
                    'fast auto'
                )
            ],
            (
                Omega
                    | '''keep
  indent'''
                    | Alpha
                    | 27,
                '''a
b
c'''
                    * 778
                    * true
                    * {fn(fn(x + 1))},
                nil
                    * never,
                never
                    | torch.Tensor
            )
        ]
    )

Plan2[S] =
    (
        (
            (
                never,
                MatchResult,
                325
            )
        )
            | (
                (
                    0.19,
                    '''line one
line two''',
                    Alpha,
                    {fn(fn(x + 1))}
                )
            )
            | (
                (
                    'strict'
                )
            )
    )
        * (
            Map[
                {fn(fn(x + 1))}
                    <- nil
                    <- MatchResult,
                Box[true, 'fast']
            ]
        )
        * (
            (
                false
                    * {x + 1}
            )
                <- (
                    Output
                        <- 9.91
                        <- Handler
                        <- Context
                )
                <- (
                    (
                        Error
                    )
                )
                <- (
                    {fn(x + 1)}
                        * Output
                        * never
                )
        )

Tensor4[In] =
    (
        10
            <- 732
    )
        <- (
            $env.scope ()
                * Input
                * ...
                * 8.146
        )
        <- (
            Error
                <- '''line one
line two'''
                <- false
        )
        <- (
            Gamma
                * torch.Tensor
        )

Guarded5 =
    $x int
    * Assert[{x <= 197}]
    * Delta
