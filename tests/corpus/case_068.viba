# corpus case 068 — generated by generate_corpus.py (seed 20260915)
# themes: Plan, Node, Rule, Stream, Slice

Plan1 :=
    (
        List[
            $idx 'auto slow'
        ]
    )
        | (
            $next (
                {fn(fn(x + 1))}
                    | Omega
            )
        )
        | (
            (
                fx.Graph
                    <- $env.scope
                    <- never
            )
                * (
                    'viba auto'
                        | '''a
b
c'''
                        | '''line one
line two'''
                )
        )
        | (
            (
                Metric
                    <- 929
                    <- 9.0
            )
                * (
                    'lazy slow'
                        | GraphModule
                        | never
                        | 842
                )
                * (
                    $err never
                )
                * (
                    $dst Input
                )
        )

Plan2 :=
    (
        void
            * Alpha
            * Error
    )

Slice3[V, Out] :=
    $err (
        true
            * Alpha
            * 'fast viba'
            * {fn(x + 1)}
            * (
                void
                    | Output
            )
            * (
                fx.Graph
                    | Output
            )
            * (
                (
                    134
                )
            )
            * {x + 1}
            * false
            * Handler
            * (
                true
                    | never
                    | '''line one
line two'''
                    | Error
            )
            * (
                Result[
                    {x + 1}
                        | never,
                    Alpha
                        * State
                        * 'viba fast lazy'
                        * MatchResult
                ]
            )
    )

Node5[U, K] :=
    $meta (
        Pair[
            'auto fast'
                <- Output
                <- true
                <- 9.5
        ]
    )

Guarded6 :=
    $ratio int
    * Assert[{ratio <= 80}]
    * Handler
