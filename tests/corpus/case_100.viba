# corpus case 100 — generated by generate_corpus.py (seed 20260915)
# themes: Node, Edge, Option, Graph, Stream

State1[K] :=
    (
        (
            $next Alpha
        )
            * 890
            * Context
            * (
                Metric
                    | {fn(fn(x + 1))}
                    | 2.653
                    | Alpha
            )
            * (
                962
                    | {fn(fn(x + 1))}
                    | {fn(fn(x + 1))}
                    | 3.6
            )
    )
        | (
            'viba fast strict'
                <- 364
        )
        | (
            Context
                * ...
                * 'lazy slow'
                * ...
        )

Batch2 :=
    $next (
        {fn(x + 1)}
            * 'strict lazy'
            * 488
            * Error
            * Metric
            * void
            * 552
            * (
                List[$meta.id, Alpha, {fn(fn(x + 1))}]
            )
    )

Graph4 :=
    (
        List[
            {fn(fn(x + 1))}
                * Sigma
                * true,
            {fn(fn(x + 1))}
                <- 'viba',
            720
                * $meta.id
                * ...
        ]
    )
        * (
            $cfg (
                $input 8.9
            )
        )
        * (
            $idx Beta
        )
        * Beta
        * never

Plan5[E] :=
    $output (
        (
            Option[
                (
                    'lazy slow auto'
                ),
                State
                    * '''a
b
c'''
                    * GraphModule
                    * $meta.id
            ]
        )
            * (
                $idx (
                    (
                        State
                    )
                )
            )
    )

Tensor7[Out, V] :=
    (
        $meta Config
    )
        | (
            (
                ...
            )
        )
        | (
            $cfg 'slow'
        )

Guarded8 :=
    $lines float
    * Assert[{lines <= 186}]
    * Metric
