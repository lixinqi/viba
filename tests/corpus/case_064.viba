# corpus case 064 — generated by generate_corpus.py (seed 20260915)
# themes: Result, Report, Handler, Task, Stream

Edge1 :=
    (
        $input (
            510
                | Input
                | {fn(x + 1)}
        )
    )
        * (
            $output (
                $dst Alpha
            )
        )
        * 995
        * Context
        * {fn(x + 1)}

Plan2[Out, In] :=
    (
        $src true
    )
        * (
            (
                'viba fast auto'
            )
        )
        * never
        * 56
        * false
        * (
            State
                <- Alpha
                <- Input
                <- Omega
        )

Result3[E, S] :=
    (
        (
            $idx 6.0
        )
            | (
                $res.val
                    <- never
                    <- 'slow viba'
            )
            | (
                Pair['fast viba strict']
            )
    )
        * (
            $name $res.val
        )
        * State
        * 'auto slow'
        * Input
        * (
            ...
                | '''line one
line two'''
                | '''a
b
c'''
                | ...
        )

Pair4[E, T] :=
    (
        $src true
    )
        <- (
            Omega
                <- true
                <- fx.Graph
                <- never
        )
        <- (
            'lazy viba fast'
                * MatchResult
                * Sigma
        )

Rule5[In] :=
    Option[
        $value Error
    ]

Result6[S, E] :=
    (
        Context
            | 4.13
            | true
            | 210
    )
        * {fn(x + 1)}
        * 'strict'

Handler7[E] :=
    (
        7.349
            <- fx.Graph
    )

Check9[T, V] :=
    Alpha
        | fx.Graph
        | Output
        | Alpha
