# 路线：跨时空执行

viba 是本体论：定义说的是"世界里有什么、怎么寻址"，不说的是"谁来算"。这一章讲这条分界线能推到
多远——一份 viba 程序的前半段在一台 Go 服务上跑，后半段在一个 TypeScript 写的网页上跑；同一个
程序今天在节点 A 上跑一半，明天在节点 B 上接着跑；也可以在一个 agent 手里开始，在另一个 agent
手里结束。这些不是四件事，是同一件事。

今天还没有第二个 `interpret` 实现，也还没有跨进程的调度器。所以这一章写的是方向：哪些零件已经在
位，哪些还没写，以及哪几件事必须先定下来。

## 1. 一句话：执行是（程序，store）的函数

只要这一点成立，"跨时空"就不必另做一套机制。

一次运行要的就两样东西：**程序文本**和 **store**（`EnvironmentStorage` 那套读写——快照、Prepare、
证据）。换个系统就是把这两样喂给另一个 `interpret`；隔一年就是把它们喂给未来的那个 `interpret`。
中间不需要搬运执行状态——**continuation 没有被捕获，它就在盘上**：能续跑到哪一步，取决于 store
里已经有多少实例。

于是"执行"和"回放"是同一段代码：宿主函数用 `replayed` 写下答案（[`viba-interpreter.md`](viba-interpreter.md)），
下一次重跑读到它，那条不纯的路一次都不走。接力的动作因此可以朴素到只是**再跑一遍**——这次 store
更满，所以走得更远。

两个轴是同一个性质：

| | 换位置 | 换时间 |
|---|---|---|
| 怎么换 | 另一门语言的 `interpret` 读同一份 store | 未来的运行时读同一份 store |
| 靠什么 | 地址在两边指的是同一件事 | 实例在本体上仍然可读、可判 |

时间那一轴真正的收获在实例本身：快照是 `serialize` 写出的 viba 源码（`Name = …`），不是二进制
日志——当初跑它的代码没了，人也读得懂，模型也读得懂，类型层还能再判一遍。

"谁去算"在语言里已经有了位置：`interpret` 的答案有四支——`$ok`、`$viba_program_err`、`$underlying_viba_op_failed Failure`，
以及 `$not_my_duty_exception Duty`。后两支都带着 `$step`（哪条路径上的哪个定义），递延还带着
`$call`（它拿到的实例）；`$not_my_duty_exception` 就是"这一步不在这台机器上作答"，它让"跑到一半"
成为一个正常的答案，而不是一次失败（[`viba-interpreter.md`](viba-interpreter.md)）。

## 2. 本体这一侧：零件已经都在

把现有模块按本体引擎的角色摆一遍，会看到这里没有缺口：

| 问题 | 今天在哪 |
|---|---|
| 世界里的数据怎么接上来 | 六格访问接口，[`viba/reflect.py`](viba/reflect.py)（[`viba-reflect.md`](viba-reflect.md)） |
| 这份类型能不能被反射穿 | [`viba/is_complete.py`](viba/is_complete.py) |
| 一个东西能不能被唯一寻址 | [`viba/check_tag_and_inline.py`](viba/check_tag_and_inline.py) |
| 交回来的实例顶不顶得上那个槽位 | [`viba/is_sub_type.py`](viba/is_sub_type.py) |
| 这一次回答算不算数 | 规则与呈证，[`viba-compliance.md`](viba-compliance.md) |
| 程序怎么跑、结果存哪 | [`viba/interpret.py`](viba/interpret.py) |

跨系统的集成通常是"把数据搬过去、转成对方的 schema"。这里反过来：**数据不动，加一层寻址把它接到
本体上**；系统之间传的是**工单**（地址加实例），不是数据副本。这层链接还是懒的：契约只要求"类型里
列得出的每个地址，在数据里都走得通"，数据可以多过类型——每个系统只需要把工单问到的那一部分物化
出来。

## 3. 一次接力长什么样

分界不在"前半段/后半段"这种位置描述上，而在**纯与不纯**上：

- **纯净的骨架**可以在任何地方重算。它没有时钟、没有随机、不调服务，重算多少遍都是同一个结果，
  所以谁算都一样，换机器、隔一年都对得上。
- **有副作用的叶子**只能在某一处算一次，然后被记下来。它可能读时钟、掷骰子、调外部服务。

于是接力是这样一圈，每一步今天都有对应物：

1. 在 A 上跑程序，一直走到一个**本地答不出来的调用**——run 停在那里，答案就是
   `$not_my_duty_exception Duty`；
2. 答案里就带着 `$step` 与 `$call`，照它写工单——`$call` 加上待填的 `$measured`，落在
   `$step.module_path` 那条路径下的 `prepare/<名字>.viba`（[`viba/compliance/`](viba/compliance/)）。
   地址来自答案本身，工单因此自己带着身份；
3. 交给 B：另一门语言、另一个节点、另一个 agent；
4. B 回答，答案落进同一份 store 的同一个地址；
5. A 再跑一遍——这次回放命中，程序接着往下走。

`replayed` 让第 1 步和第 5 步是同一段代码，这就是幂等在这里的用处：**重跑即续跑**。

## 4. 五件承重的事

**一、自动递延只对"我没有这个能力"安全，不能对"我有、但我失败了"生效。** 两者在 interpret 里是
分开的两支：`get_func` 返回 `None`（或它自己抛递延）是"不是我的"，answer 是
`$not_my_duty_exception Duty`；宿主抛别的异常是"是我的、坏了"，answer 是 `$underlying_viba_op_failed Failure`。
两支都带着 `$step`，所以递延可以放心自动传下去，失败则不必。守住这条线，才不会把一次真失败变成
"换台机器重做一次副作用"。

**二、路由键是 storage 路径，不是函数名。** 宿主拿到的是 `get_func(module_path, func_name)`，其中
`module_path` 就是这次调用所在环境的路径：同一个 `distance` 出现在 `root/case_1230` 下和在
`root/case_9999` 下，是两件不同的工单。由此有三条读法：

- `environ.sub_env << "名字"` 在这个语境里该读成一张**租约**：谁拿着这个案子，谁写这个子路径；
- `environ.tmp_sub_env << ()` 是本体里现成的"**别缓存**"标记：就地算完就算完，不产生交接件；
- "两次模块调用不许同用一条路径"那条错，在分布式里说的是**身份**——同一条路径上两个系统同时写，
  就是丢更新。

**三、能过界的只有实例。** 宿主对象（environment、实例句柄）过不了任何序列化边界，而每个可执行
函数都带 `$env Environment`。所以递延的边界必须落在**参数全是实例**的叶子上，接收方**自己造**它
那一侧的 environment——这恰好就是"每门语言各有自己的 `interpret`"的含义：环境是局部的，不随工单
走。要让工单自足，地址本身也得是本体可见的实例：`$viba_path` 已经是
[`viba/builtin.viba`](viba/builtin.viba) 里 `Environment` 的一个 str 成员，storage 路径目前还只在
宿主侧。

**四、可以只实现部分算子，但核心归约必须完整。** 要"走到边界再递延"，得先能走到那儿：模块加载、
`<<` 的归约、值物化、环境构造。这一层每门语言都得有（好在语言很小，可移植），各系统不同的是
**能力表**，不是语义。若某门语言只实现了半个核心，递过去的就不是实例，而是没算完的语法，边界也
就糊了。

**五、递延带回来的是"哪一步"，还没带"谁接着做"。** `$step`、`$call`、`$reason` 都随递延回来了，
所以工单能直接写出来；没定的是**路由**：谁接、按什么规则接、同一条地址上谁有写权。这一层也可以用
本体写：能力表是一份实例，派单是一份程序，规则判它。

## 5. agent 与节点是同一种宿主

在这套东西里，"谁来补实现"从来不是一个额外问题：viba 对每个要实现的算子只给一句提示，实现是照着
提示写出来的（[`viba-interpreter.md`](viba-interpreter.md)）。所以一个 agent 就是一个宿主，只是它
的能力表和别的宿主不同：

- `{…}` 那句提示就是**工单的说明**；接口被类型钉死（槽位、tag、结果的类型、`$env` 那一格），agent
  的自由只落在叶子内部；
- 不确定性被 `replayed` 关进叶子：agent 可以掷骰子，但答案只产生一次，之后整条程序照旧可复现；
- 接力不必传上下文——**本体就是共享上下文**，任何一个 agent 拿到任一未完成的工单都能上手；
- 也不必"信任某个 agent"：`measure` 加一份规则程序就是验收，换一个 agent 复核不改协议。

节点之间的接力是同一件事换一种宿主：能力表从"某个 agent 会什么"变成"某台机器、某门语言会什么"。

## 6. 和已有做法的关系

最接近的工业形态是 durable execution（工作流引擎那一类）：都靠回放把长流程接起来。差别在两处，
而这两处正是这条路线的意思所在：

- 那边的历史是一份专用的日志，可靠性靠"代码不许不确定"这条纪律；这里的记忆是**本体化的实例**——
  人能读、模型能读、类型层能再读一遍；
- 那边的"代码"是一个程序，不确定的地方靠约定绕开；这里的"代码"是定义，有副作用的叶子是**显式
  留白**，留白处写着它要做什么。

和记忆化构建也像：都是 (谁做、做什么) 命中就跳过。区别在键和值——键是语义地址（案子的路径加这一
步的名字），值是本体上的实例：可以被检查、被判定、被重新定型，而不是一个不透明的摘要。

和 IDL／代码生成的区别更直接：这里没有"生成桩代码"这一步。接口就是定义，合规在运行时按结构判：
覆盖度问 `is_complete`，槽位匹配问 `is_sub_type`，准入问规则。

## 7. 会咬人的地方

- **地址的版本**：类型改了，同一个地址下的旧答案还算不算数？描述符里的 `$file_hash` 是种子，但
  地址里得有版本的位置，否则"同一条路径"会在两个含义之间滑动。
- **恰好一次**：副作用与快照落盘之间有一个窗口，中间挂掉，重跑就会重做那一步。要恰好一次，就得
  在动手之前先落工单——`prepare` 的写法已经是对的方向，但它得从写法变成纪律。
- **store 是协调点**：每个跨系统步骤都是一次写，所以按路径的原子性是前提；最省事的做法是单写者，
  也就是上面那张租约。
- **静默发散**：回放只在叶子处兜住不确定性。一个既不确定、又没走 `replayed` 的宿主函数，会让两次
  运行悄悄不同。所以"能力"最好带上注册信息：这一项是纯的，还是快照的。

## 8. 第一步

不用动语言，也不用先写第二个 `interpret`：

1. 两个 Python 进程（当两门语言用），一个共享的 store 目录；
2. 一个只做一件事的路由器：读到递延，用它的 `$step` 与 `$call` 写出工单，然后退出；
3. 另一端读到工单、回答、落盘；
4. 原来那一端重跑，应当命中回放，一直跑到终点。

判据是：第一次运行留下的只有工单，第二次运行不重走任何一条不纯的路，而结果与"全在一台机器上跑"
完全一致。这一圈走通，剩下的就是把一侧换成 Go 或 TypeScript 的 `interpret`，用同一个 store 再走
一遍。
