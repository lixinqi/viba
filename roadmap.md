# 路线：viba 作为本体

viba 不回答"谁来算"。它只回答两件事：**世界里有什么，以及一个东西该怎么寻址。**

一个 viba 文件是一份定义——类型、字段、分支、函数的写法。它不写实现，不写执行，不指定哪台机器上跑。这让它不是一个业务 DSL，而是一个**本体**：一张被所有 agent、所有系统、所有语言共享的世界地图。

一份定义的前半段在 Go 服务上跑，后半段在 TypeScript 网页上跑；同一个程序今天在节点 A 上跑一半，明天在节点 B 上接着跑；一个 agent 起头，另一个 agent 收尾。这些不是三件事，是一件——本体不绑执行，执行只是本体的一次投影。

今天还没有第二个 `interpret`，也没有跨进程调度器。于是问题落在四件事上：本体哪一面已经成型，接力怎么发生，哪几件事是承重的，从哪里开始。

## 1. 本体自己不做任何一头

viba 把三类交流翻译成同一种语言——类型。它自己只是个转换器，谁也不当：

- **人 ↔ agent，走 `{...}`。** 人说意图不必碰箭头——箭头是 agent 之间钉契约的语言。它是意图的自然语言载体，不参与类型、不被执行；agent 照着它补出实现。
- **agent ↔ agent，走 ADT。** 和、积、指数、子类型、type/instance 二分——这是两边交付物的契约。它强制精确、机器可判，不靠对话去猜。
- **接传统软件，走 `<<`。** 类型层上它是偏应用，运行时上它就是函数调用。viba 不靠新造调用语法，把"给参数"这件事在两头钉成同一个符号。

所以 viba 不跑业务、不存数据、不调度。它把意图、契约、执行都落到类型上，让三者说同一种话。

本体不只是把世界描述下来。它真正要做的是持续发现漂移：实现和设计差了多少、一次运行违没违规、两次跑是不是悄悄不同。描述是为了能比对，比对是为了发现离群点——这就是对抗熵增的前提。

## 2. 本体的零件：都已就位

本体要用到的零件，今天一个都不缺：

| 本体要回答的问题 | 今天在哪 |
|---|---|
| 世界里的数据怎么接上来 | 六个访问函数（`root` / `has` / `get` / `leaf` / `length` / `keys`），[`viba/reflect.py`](viba/reflect.py)（[`viba-reflect.md`](viba-reflect.md)） |
| 一份定义能不能被反射穿 | [`viba/is_complete.py`](viba/is_complete.py) |
| 一个东西能不能被唯一寻址 | [`viba/check_tag_and_inline.py`](viba/check_tag_and_inline.py) |
| 交回来的实例顶不顶得上那个槽位 | [`viba/is_sub_type.py`](viba/is_sub_type.py) |
| 这一次回答算不算数 | 规则与呈证，[`viba-compliance.md`](viba-compliance.md) |
| 程序怎么跑、结果存哪 | [`viba/interpret.py`](viba/interpret.py) |

这些零件合起来是一台离群点探测器：`is_sub_type` 发现交回来的实例顶不上槽位，`compliance` 发现一次运行违没违规，`replayed` 发现两次运行悄悄发散。没有一个是"保证不出错"，全是"这里对不对"。

跨系统集成就反过来做：**数据不动，加一层寻址把它接到本体上。** 系统之间传的是工单（地址加实例），不是数据副本。这层链接还是惰性的：契约只要求定义里列得出的每个地址在数据里都走得通，数据可以多过定义——每个系统只需把工单问到的那部分物化出来。

## 3. 接力：本体怎么被跨时空使用

分界不在"前半段/后半段"的位置描述上，在**纯与不纯**上：

- **纯净的骨架**在哪都能重算。没有时钟、没有随机、不调服务，重算多少遍都是同一个结果，谁算都一样，换机器、隔一年都对得上。
- **有副作用的叶子**只能在某一处算一次，然后被记下来。它可能读时钟、掷骰子、调外部服务。

接力是这样一圈，每一步今天都有对应物：

1. 在 A 上跑，走到一个**本地答不出来的调用**——run 停在那里，答案就是 `$not_my_duty_exception Duty`；
2. 答案带着 `$step` 与 `$call`，照它写工单：`$call` 加待填的 `$measured`，落在 `$step.module_path` 那条路径下的 `prepare/<名字>.viba`（[`viba/compliance/`](viba/compliance/)）。地址来自答案本身，工单自己带着身份；
3. 交给 B：另一门语言、另一个节点、另一个 agent；
4. B 回答，答案落进同一份 store 的同一个地址；
5. A 再跑一遍——这次回放命中，程序接着往下走。

`replayed` 让第 1 步和第 5 步是同一段代码：**重跑即续跑。**

中间不需要搬执行状态——continuation 没有被捕获，它就在盘上。能续跑到哪一步，取决于 store 里已经落了多少实例。

时间那一轴真正的收获在实例本身：快照是 `serialize` 写出的 viba 源码（`Name = …`），不是二进制日志——当初跑它的代码没了，人也读得懂，模型也读得懂，类型层还能再判一遍。

## 4. 五件承重的判断

**一、自动递延只对"我没有这个能力"安全，不对"我有、但我失败了"生效。** 两者在 interpret 里是分开的两支：`get_func` 返回 `None`（或它自己抛递延）是"不是我的"，答案是 `$not_my_duty_exception Duty`；宿主抛别的异常是"是我的、坏了"，答案是 `$underlying_viba_op_failed Failure`。两支都带着 `$step`，所以递延可以放心自动传下去，失败则不必。守住这条线，才不会把一次真失败变成"换台机器重做一次副作用"。

**二、路由键是 storage 路径，不是函数名。** 宿主拿到的是 `get_func(module_path, func_name)`，其中 `module_path` 就是这次调用所在环境的路径：同一个 `distance` 出现在 `root/case_1230` 下和在 `root/case_9999` 下，是两件不同的工单。由此有三条读法：

- `environ.sub_env << environ << "名字"` 在这个语境里该读成一张**租约**：谁拿着这个案子，谁写这个子路径；
- `environ.tmp_sub_env << environ` 是本体里现成的"**别缓存**"标记：就地算完就算完，不产生交接件；
- "两次模块调用不许同用一条路径"那条错，在分布式里说的是**身份**——同一条路径上两个系统同时写，就是丢更新。

**三、能过界的只有实例。** 宿主对象（environment、实例句柄）过不了任何序列化边界，而每个可执行函数都带 `$env Environment`。所以递延的边界必须落在**参数全是实例**的叶子上，接收方**自己造**它那一侧的 environment——这恰好就是"每门语言各有自己的 `interpret`"的含义：环境是局部的，不随工单走。要让工单自足，地址本身也得是本体可见的实例：`$viba_path` 已经是 [`viba/builtin.viba`](viba/builtin.viba) 里 `Environment` 的一个 str 成员，storage 路径目前还只在宿主侧。

**四、可以只实现部分算子，但核心归约必须完整。** 要"走到边界再递延"，得先能走到那儿：模块加载、`<<` 的归约、值物化、环境构造。这一层每门语言都得有（好在语言很小，可移植），各系统不同的是**能力表**，不是语义。若某门语言只实现了半个核心，递过去的就不是实例，而是没算完的语法，边界也就糊了。

**五、递延带回来的是"哪一步"，还没带"谁接着做"。** `$step`、`$call`、`$reason` 都随递延回来了，所以工单能直接写出来；没定的是**路由**：谁接、按什么规则接、同一条地址上谁有写权。这一层也可以用本体写：能力表是一份实例，派单是一份程序，规则判它。

## 5. agent 与节点是同一种宿主

在这套东西里，"谁来补实现"从来不是一个额外问题：viba 对每个要实现的算子只给一句提示，实现是照着提示写出来的（[`viba-interpreter.md`](viba-interpreter.md)）。所以一个 agent 就是一个宿主，只是它的能力表和别的宿主不同：

- `{…}` 那句提示就是**工单的说明**；接口被类型钉死（槽位、tag、结果的类型、`$env` 参数），agent 的自由只落在叶子内部；
- 不确定性被 `replayed` 关进叶子：agent 可以掷骰子，但答案只产生一次，之后整条程序照旧可复现；
- 接力不必传上下文——**本体就是共享上下文**，任何一个 agent 拿到任一未完成的工单都能上手；
- 也不必"信任某个 agent"：`measure` 加一份规则程序就是验收，换一个 agent 复核不改协议。

节点之间的接力是同一件事换一种宿主：能力表从"某个 agent 会什么"变成"某台机器、某门语言会什么"。

接力这一层是平权的——任何 agent 拿到工单都能上手，不必信任谁。但定描述、审描述是另一层：高权限 agent 设计和总结 viba 描述（立法）、审核它有没有改坏（司法），低权限 agent 只跑校验、把实现和设计的差异压到最小（行政）。发现离群点靠的是那台探测器，处置它靠的是这层分权——熵增不是靠规则写得完美挡住的，是靠"定规则的人"和"守规则的人"分开挡住的。

## 6. 和已有做法的关系

最接近的工业形态是 durable execution（工作流引擎那一类）：都靠回放把长流程接起来。差别在两处，而这两处正是这条路线的意思所在：

- 那边的历史是一份专用的日志，可靠性靠"代码不许不确定"这条纪律；这里的记忆是**本体化的实例**——人能读、模型能读、类型层能再读一遍；
- 那边的"代码"是一个程序，不确定的地方靠约定绕开；这里的"代码"是定义，有副作用的叶子是**显式留白**，留白处写着它要做什么。

和记忆化构建也像：都是 (谁做、做什么) 命中就跳过。区别在键和值——键是语义地址（案子的路径加这一步的名字），值是本体上的实例：可以被检查、被判定、被重新定型，而不是一个不透明的摘要。

和 IDL／代码生成的区别更直接：这里没有"生成桩代码"这一步。接口就是定义，合规在运行时按结构判：覆盖度问 `is_complete`，槽位匹配问 `is_sub_type`，准入问规则。

## 7. 会出错的地方

- **地址的版本**：定义改了，同一个地址下的旧答案还算不算数？描述符里的 `$file_hash` 是种子，但地址里得有版本的位置，否则"同一条路径"会在两个含义之间滑动。
- **恰好一次**：副作用与快照落盘之间有一个窗口，中间挂掉，重跑就会重做那一步。要恰好一次，就得在动手之前先落工单——`prepare` 的写法已经是对的方向，但它得从写法变成纪律。
- **store 是协调点**：每个跨系统步骤都是一次写，所以按路径的原子性是前提；最省事的做法是单写者，也就是上面那张租约。
- **静默发散**：回放只在叶子处兜住不确定性。一个既不确定、又没走 `replayed` 的宿主函数，会让两次运行悄悄不同。所以"能力"最好带上注册信息：这一项是纯的，还是快照的。

## 8. 从哪里开始

不用动语言，也不用先写第二个 `interpret`：

1. 两个 Python 进程（当两门语言用），一个共享的 store 目录；
2. 一个只做一件事的路由器：读到递延，用它的 `$step` 与 `$call` 写出工单，然后退出；
3. 另一端读到工单、回答、落盘；
4. 原来那一端重跑，应当命中回放，一直跑到终点。

判据是：第一次运行留下的只有工单，第二次运行不重走任何一条不纯的路，而结果与"全在一台机器上跑"完全一致。这一圈走通，剩下的就是把一侧换成 Go 或 TypeScript 的 `interpret`，用同一个 store 再走一遍。
